fun fibonacci() : nothing

	var i : int;
	
	fun fib(n : int) : int
	{
		if n < 2 then
			return n;
		else
			return fib(n - 2) + fib(n - 1);
	}
{
	i <- 0;
	
	while i < 45 do
	{
		puti(fib(i));
		puts("\n");
		i <- i + 1;
	}
}
